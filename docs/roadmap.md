# Hitalk v2 路线图

更新日期：2026-09-18。前端、API 和 D1 已部署并进入试运行，旧系统评论已完成一次性导入。

原重写方案已经转化为可运行代码。当前优先级是把既有功能维护好，避免继续扩展未经验证的约定。

## 已完成

- [x] 统一鉴权，缺失配置拒绝管理访问，移除 URL 凭据
- [x] 分离公开响应与数据库模型，保护邮箱/UA/IP 标识
- [x] 输入校验、HTML 属性转义与管理页安全渲染；只保存 Markdown；采集 UA 用于浏览器/系统展示，不采集评论 IP
- [x] 数据库约束保证回复归属和无环，触发器维护级联计数
- [x] 点赞去重与事务内计数结果
- [x] SDK 草稿/回复/点赞状态维护、请求取消、销毁和重新挂载
- [x] 根评论分页、失败提示和重新加载
- [x] SDK ESM/IIFE 与独立类型声明
- [x] 版本化迁移、自动化测试、CI 和隔离备份恢复演练
- [x] 更新使用、首次部署与备份恢复文档
- [x] GitHub Pages 绑定 `hitalk-next.ihoey.com`，Worker 绑定 `hitalk-next-api.ihoey.com`
- [x] 初始化独立生产 D1 `hitalk-next`，导入旧系统 717 条评论及 UA
- [x] SDK 样式分组、状态提示和头像 hash 修复

## 当前阶段：试运行与维护

- [ ] 将本地开发与线上数据预览明确分开，恢复可配置的 API 地址
- [ ] 对齐本地 seed 的 `/playground` 与示例当前使用的 `/`
- [ ] 配置匿名写入防滥用策略
- [ ] 用真实浏览器验收博客主题/移动端/SPA 集成
- [ ] 若启用邮件，验证域名、发件人和实际投递
- [ ] 用真实流量决定分页、限流和日志后续工作

## 暂缓

登录系统、更多插件、复杂后台、国际化、大规模 UI 重构以及通用数据库抽象。旧系统评论已完成一次性迁移；不维护旧 API/SDK 的兼容层，也不提供通用迁移工具。

边界与限制见 [维护说明](architecture.md)，上线流程见 [运维说明](operations.md)。
