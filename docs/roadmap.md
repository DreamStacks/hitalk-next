# 实施与上线状态

更新日期：2026-09-18。原计划 10 项实施任务及后续上线验收均已完成，当前进入日常维护阶段。重构使用新 schema 和唯一 /api 协议，不保留旧协议或字段兼容逻辑。

已实现：

- [x] 本地示例默认连接本地 Worker；使用独立本地数据库标识。
- [x] 浏览器持久匿名身份，服务端摘要存储，多标签页初始化协调。
- [x] 作者删除自己的内容，保留他人回复；按身份点赞/取消点赞。
- [x] 评论创建幂等、并发首次身份创建、超时重试及删除后重放。
- [x] 平铺线程模型、根/回复游标分页、有限预览和按需定位。
- [x] 审核、隐藏、垃圾标记、作者待审核回执、页面开关及身份封禁。
- [x] 基础身份/IP 频控，不使用验证码。
- [x] D1 持久通知任务、租约、固定载荷、失败重试、退订和取消。
- [x] SDK 请求取消、草稿/请求标识恢复、局部合并及迟到响应保护。
- [x] 管理端、真实 D1 测试、发布产物与备份检查同步采用新模型。

- [x] 评论资料折叠、输入项 name、分类型反馈、主题切换、卡片淡入及关闭评论状态完善。
- [x] 示例页 CSS 提前加载、SVG 固定尺寸，首屏大 logo 闪烁修复并发布。
- [x] 开发、测试、构建、部署和数据库维护脚本整理。
- [x] Worker API、管理端和 GitHub Pages 示例页已部署；生产密钥和地址配置完成。
- [x] 旧库一次性迁移至 hitalk-next-v3：28 个页面、718 条评论（含 252 条回复）、2 条点赞；备份、逐字段核对和线上接口检查通过，旧库保留。上述数量为迁移快照。
- [x] 生产邮件开启并沿用旧文案与样式；用户于 2026-09-18 确认真实邮件验证通过，不另行推定具体测试场景。
- [x] Hexo 博客接入本地托管 SDK、生产 API、文章计数及历史评论 ID 链接定位。
- [x] 博客通过 Vercel 的 master 静态分支发布；首页、留言板、文章页和静态资源验证通过。
- [x] 用户确认本轮页面及博客接入正常，并授权发布；计划与交付记录已更新。

生产入口：博客 https://blog.ihoey.com/，示例 https://hitalk-next.ihoey.com/，API https://hitalk-next-api.ihoey.com/api，管理端 https://hitalk-next-api.ihoey.com/admin。

验证记录：SDK 最近功能版本 a522c90 的 66 项常规测试、Lint 和类型检查通过；真实 Worker/D1 集成测试及导出恢复检查在前序发布阶段通过。博客源码版本 e73dfac8，生产静态版本 70a0f2e8。文档更新不重复运行代码测试。

原计划没有未完成的上线阻塞项。后续按使用反馈维护；定期备份、关注失败通知任务和依赖更新属于维护建议，尚未配置定时自动化。两条历史评论对应的旧页面不在当前博客源码中，数据保留，不擅自重定向或合并。

当前不包含：密码账户、邮箱认领身份、跨设备同步、作者编辑历史、文件上传、举报工作流、多租户、完整投递回执跟踪。
